package cz.cuni.mff.respefo;

import org.eclipse.swt.graphics.Color;
import org.eclipse.swt.widgets.Display;

public class ChartBuilder {
	private static final Color black = new Color(Display.getDefault(), 0, 0, 0);
	private static final Color green = new Color(Display.getDefault(), 0, 255, 0);
	private static final Color blue = new Color(Display.getDefault(), 0, 0, 255);
	private static final Color yellow = new Color(Display.getDefault(), 255, 255, 0);
}
